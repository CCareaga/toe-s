#include "include/llist.h"
#include "include/vga.h"

void add(bin_t *bin, node_t* node) {

}

void remove(bin_t * bin, node_t *node) {

}

node_t *next(node_t *current) {
    return current->next;
}

node_t *prev(node_t *current) {
    return current->prev;
}
